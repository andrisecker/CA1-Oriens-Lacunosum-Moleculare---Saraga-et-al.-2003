load_file("nrngui.hoc")
load_file("SaragaOLM_Case2.hoc")

// ======================================================

objectvar OLMcells[1]

OLMcells[0] = new OLMSaraga_Case2()

v_init = -65
t = 0
dt = 0.001
steps_per_ms = 1000
tstop = 200

objectvar stim[1]

OLMcells[0].soma {
    stim[0] = new IClamp(0.5)
    stim[0].del = 0
    stim[0].dur = 200
    stim[0].amp = 0.1
}

// ======================================================

load_file("dump.hoc")
